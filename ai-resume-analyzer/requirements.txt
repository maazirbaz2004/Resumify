#       AI CV ANALYZER PRO — requirements.txt
# __________________________________________________

# Install all dependencies:
#   pip install -r requirements.txt
#
# If you are on Windows and torch gives an error, install it separately first:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
#   then: pip install -r requirements.txt
# ______________________________________________________________________________


# ── Web framework ─────────────────────────────────────────────────────────────
streamlit>=1.32.0

# ── Data handling ─────────────────────────────────────────────────────────────
pandas>=2.1.0
numpy>=1.26.0

# ── Machine learning ──────────────────────────────────────────────────────────
scikit-learn>=1.4.0          # LogisticRegression, LinearSVC, LabelEncoder, metrics
joblib>=1.3.0                # model serialisation (save/load classifier)

# ── Deep learning / embeddings ────────────────────────────────────────────────
# torch is a dependency of sentence-transformers — listed explicitly for clarity
torch>=2.1.0
sentence-transformers>=2.6.0 # SentenceTransformer, util.semantic_search

# ── NLP utilities ─────────────────────────────────────────────────────────────
nltk>=3.8.0                  # stopwords for text cleaning

# ── Visualisation ─────────────────────────────────────────────────────────────
plotly>=5.18.0               # all charts (gauge, confusion matrix, bar charts)

# ── PDF parsing ───────────────────────────────────────────────────────────────
PyPDF2>=3.0.0                # extract text from uploaded PDF resumes